{
  "name": "sketches",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm-run-all --parallel dev:*",
    "dev:eleventy": "eleventy --serve",
    "dev:js": "esbuild --watch src/js/entries/* src/js/load.js --outdir=_site --outbase=src/js  --sourcemap --minify",
    "dev:tsc": "tsc -noEmit",
    "prod:eleventy": "eleventy",
    "prod:js": "tsc -noEmit && esbuild src/js/entries/* src/js/load.js --outdir=_site --outbase=src/js --minify",
    "build": "run-s prod:*",
    "clean": "rm -rf _site .cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@types/p5": "^1.3.0",
    "esbuild": "^0.12.22",
    "npm-run-all": "^4.1.5",
    "nunjucks": "^3.2.3",
    "typescript": "^4.3.5"
  }
}
