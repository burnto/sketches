---
layout: "base.njk"
pagination:
    addAllPagesToCollections: true
    data: entries
    size: 1
    alias: currentEntry
eleventyComputed:
  title: "{{ currentEntry.title }}"
  thumbnail: "{{currentEntry.thumbnail}}"
permalink: "/{{ currentEntry.baseFilename }}/"
---

{% include 'entry.njk' %}

