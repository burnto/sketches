<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="Brent Fitzgerald">
    {% if thumbnail %}
      <meta property="og:image" content="{{thumbnail}}" />
    {% endif %}
    <title>{% if title %}{{ title }} - {% endif %}bloc.art</title>
    <style>
      html, body {
        text-align: left;
        font-family: monospace;
        margin: 0;
        line-height: 1.8;
        background-color: black;
      }

      a {
        color: black;
      }

      a:hover {
        color: black;
        background-color: yellow;
      }

      footer {
        margin-top: 50px;
        color: #aaa;
      }

      footer a {
        color: #aaa;
      }

      footer strong {
        color: #888;
      }

      #entry #caption {
        display: none;
      }
      #entry {
        background-color: black;
      }

      canvas {
        display: block;
      }

      .embed canvas {
        cursor: pointer;
      }

      #index {
        margin: 20px;
      }

      #index iframe {
        width: 500px;
        height: 500px;
        border: 1px solid #ccc
        cursor: pointer;
      }

    </style>
  </head>
  <body>
    <main>
      {{ content | safe }}
    </main>
  </body>
</html>